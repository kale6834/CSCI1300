#include <iostream>
#include <string>
using namespace std;

/**
 * Algorithm: Write the code to declare the four arrays listed below. You are 
 * not writing a function; your code will be placed directly in to our main() 
 * function for evaluation. For testing/development in cloud9, simply write the 
 * code in your own main() function.
 * 1. write a set of functions
 * 2. first function will have an array of integers of 49 elements
 * 3. second function will have an array of 4 floating point values
 * 4. third function will have an array of 14 boolean values
 * 5. fourth function will have an array to hold names of the month (12)
 * Input parameters: none
 * Output parameters: none
 * Returns: 0
 */
 
int main ()
{
    int arrayOne [49]; 
    float arrayTwo [4];
    bool arrayTri [14];
    string arrayFor [12];
    return 0;
}